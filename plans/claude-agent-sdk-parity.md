# Claude Agent SDK vs OpenRouter Agent Harness — Parity Analysis

> Comparison of the [Claude Code Agent SDK](https://code.claude.com/docs/en/agent-sdk/overview) (TypeScript) against the openrouter-agent-harness feature set.
> Originally generated 2026-05-21. **Last reviewed against this codebase: 2026-05-26** (Phase 5 complete; Phase 6 comparative parity harness main sequence complete — 6.S1 + 6.1 → 6.9 all merged; Phase 6.9 rolling backfill cohort 5/20 merged then paused at #160 — scenarios #17 effort passthrough, #18 persistSession:false, #19 allowed/disallowed-tools grammar, #20 enhanced bash, #21 NotebookEdit). No library-surface changes since the 2026-05-24 snapshot — every commit since has landed under `src/__tests__/comparative/`. The parity matrix below is now backed by 21 comparative scenarios that run both SDKs against an in-process emulated wire (see [`comparative-parity-harness.md`](./comparative-parity-harness.md)). Roadmap detail in [`claude-sdk-parity-roadmap.md`](./claude-sdk-parity-roadmap.md).

## Summary

| Status             | Count  |
| ------------------ | ------ |
| Full parity        | 40     |
| Partial parity     | 5      |
| Missing            | 1      |
| **Total features** | **46** |

Net change vs the original 2026-05-21 snapshot: **+32 Full** — the additional one over the Phase 5.7 snapshot is `Plugins` after Phase 5.8. **Phase 5 is now complete**: this library reaches full parity with Claude Code + the Agent SDK on the in-scope dimensions. `Subagent hooks` stays Partial pending `SubagentStop` matcher patterns. The lone remaining "Missing" row (`Session listing ergonomics`) is the on-disk session-index helper — a quality-of-life follow-on, not a parity gap.

**Empirical verification** (Phase 6, 2026-05-25 → 2026-05-26): the matrix is no longer just a documentation claim. The `comparative` test suite (`npm run test:comparative`) runs 21 parity scenarios against an in-process emulator that speaks both Anthropic Messages and OpenAI/OR chat-completions wire formats. Each scenario drives both `@anthropic-ai/claude-agent-sdk` and `@openrouter/agent` against scripted responses with deterministic prompt hashes, then asserts event-stream equivalence (exact mode for hooks + tool firing order; tolerant mode for model-creative outputs). A nightly live-mode workflow exercises 4 of the scenarios against real provider APIs with $0.50/scenario + $0.25/PR budget caps and auto-files drift issues. 15 of the 27 originally-audited parity-row scenarios remain in the rolling backfill (`parity-scenario-backfill` label) — current pause flag is in effect on the cohort.

---

## Feature Parity Chart

### Core Agent Loop

| Feature                  | Claude Agent SDK                                                                               | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Parity   |
| ------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Autonomous tool loop     | SDK handles tool calls, results, repeats until done                                            | Delegates to `@openrouter/agent` `callModel()` SDK                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Full** |
| Turn counting            | Tracks turns, exposes in ResultMessage                                                         | Tracks turns via streaming events; surfaced on `turn_end` and `stream_complete`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Full** |
| Max turns stop condition | `maxTurns` option                                                                              | `maxTurns` constructor option → `stepCountIs()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Full** |
| Max cost stop condition  | `maxBudgetUsd` option                                                                          | `maxBudgetUsd` constructor option → `maxCost()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Full** |
| Interrupt / abort        | `query.interrupt()` and streaming-input control messages                                       | `signal` constructor option + `run.abort()` method (combined internally via `AbortSignal.any`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Full** |
| Effort / reasoning level | `effort` option (low/medium/high/xhigh/max)                                                    | `OpenRouterAgentRunOptions.effort` (Phase 5.4) — typed enum `'xhigh' \| 'high' \| 'medium' \| 'low' \| 'minimal' \| 'none'`, forwarded into the OR `callModel` call as `reasoning: { effort }` only when set. OR normalizes the level and maps to each provider's native param (OpenAI `reasoning_effort`, Anthropic `thinking.budget_tokens`, Gemini `thinkingLevel`, Qwen `thinking_budget`, xAI `reasoning_effort`); falls back to the nearest supported level when a model lacks the requested one. Per-subagent `effort` override (Phase 4.8) rides the same inheritance rails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Full** |
| Context compaction       | Automatic summarization when context fills; PreCompact hook; manual `/compact`                 | Phase 5.1 [✅ #97]: char-length-heuristic auto-trigger between runs (configurable via `compactionThreshold`, defaulting to ~80% of `getModelContextWindow(model)` translated at ~4 chars/token), partition that keeps `keepRecentTurns` trailing messages verbatim and condenses the prefix via an isolated single-shot `callModel` (sessionId `<sessionId>:compact:<uuid>`, no tools), writes back `ConversationState.messages = [summary, ...lastN]` with `previousResponseId` cleared. Manual override via `OpenRouterAgentRun.compact()`; `autoCompact: false` gates only the implicit trigger. v1 ships no tokenizer dep — token counting is a char-length heuristic biased conservatively; a real tokenizer is a follow-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Full** |
| Streaming output         | Rich message stream (SystemMessage, AssistantMessage, UserMessage, StreamEvent, ResultMessage) | `AgentCoreEvent` event stream (default) + `run.messages()` typed-message view (`AgentMessage` = SystemMessage / AssistantMessage / UserMessage / ResultMessage) after Phase 3.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Full** |
| Streaming input          | AsyncGenerator-based input; mid-session messages, interrupts, image attachments                | Phase 5.3 [✅ #104]: `OpenRouterAgentRunOptions.prompt: string \| AsyncIterable<UserInput>` widens the constructor overload — plain `string` keeps the single-shot back-compat path; `AsyncIterable<UserInput>` drives a multi-turn loop where each yielded `UserInput` is the next user message. `OpenRouterAgentRun.pushUserMessage(msg)` queues a message imperatively (unbounded FIFO buffer, drained before the iterable on each between-turn pull). `OpenRouterAgentRun.interrupt()` writes `state.interruptedBy = '''host-interrupt'''` via the run'''s `StateAccessor`; the SDK'''s `checkForInterruption` polling observes the flag between turns and exits cleanly with `status: '''interrupted'''` + populated `partialResponse`. The next streaming-input cycle commits `partialResponse.text` as an `assistant` message in `ConversationState.messages` before submitting the next user input, preserving the conversation transcript. Image attachments ride on `UserInput.content: ContentBlock[]` (lenient passthrough; OR Responses API does the Zod-level validation server-side). Interrupt granularity is between turns / between SSE event batches — not mid-token, because OR'''s wire is unidirectional SSE (spike 5.S2'''s design constraint). `messages()` rich stream stays consistent: an interrupted cycle'''s open `AssistantMessage` is flushed via a synthetic `turn_end` event. Auto-compaction (Phase 5.1) fires once at end-of-run, NOT between streaming-input turns, so the two phases don'''t race. | **Full** |

### Built-in Tools

| Feature                     | Claude Agent SDK                                      | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Parity                |
| --------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | ---------------------------------------------------------------- | -------- |
| Read file                   | `Read` tool — any file, line offsets                  | `read_file` — with `start_line`/`end_line` support                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Full**              |
| Write file                  | `Write` tool — create/overwrite                       | `write_file` — create/overwrite with auto-mkdir                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Full**              |
| Edit file                   | `Edit` tool — precise string replacement              | `edit_file` — exact unique string replacement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Full**              |
| Bash / run command          | `Bash` — full shell with description, timeout         | `run_command` — `sh -c`, 30s default timeout (configurable via `timeout_ms`, clamped to 10 min), 1MB buffer, optional advisory `description` field (Phase 3.9)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Full**              |
| Glob (file search)          | `Glob` — find files by pattern                        | `glob` — recursive find with `**`/`*`/`?`/`[a-z]`, optional `case_sensitive`, sorted output capped at 1000, BFS walk honoring `ctx.signal`, skips `node_modules`/`dist`/`coverage`/hidden (Phase 3.11)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Full**              |
| Grep (content search)       | `Grep` — regex search with context lines, file types  | `grep_files` — regex with glob filter, recursion, match limits, context lines (`before_context`/`after_context`/`context`, capped 20/side), `type` filetype filter (unions with `file_glob`), `output_mode` (`'content'`/`'files_with_matches'`/`'count'`) — Phase 3.10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Full**              |
| WebSearch                   | Built-in WebSearch tool                               | Server-side `openrouter:web_search`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Full**              |
| WebFetch                    | Built-in WebFetch tool                                | Server-side `openrouter:web_fetch`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Partial**           |
| Monitor (background script) | Watch background process, react to output lines       | `monitor` (Phase 4.4) — Zod-validated `command` + optional `cwd` / `pattern` / `max_lines` / `max_duration_ms`. Spawns `/bin/sh -c command`, pipes stdout + stderr through `readline.createInterface`, pushes each (optionally pattern-matched) line into a `{ stream, text }` buffer. Resolves on natural exit (`truncated: false`, real `exitCode`), `max_lines` cap (default 1 000, clamped to 10 000 with a `warn` notification), `max_duration_ms` cap (default 60 000, clamped to 600 000 with a `warn` notification), or `ctx.signal` abort. The three forced-stop paths SIGTERM the child with a 250ms SIGKILL grace (mirroring `run_command`) and surface `truncated: true`, `exitCode: null`. Invalid regex resolves with `{ error: 'invalid pattern: ...' }`.                                                                                                                                                                                                                                                                                                             | **Full**              |
| NotebookEdit                | Edit Jupyter notebook cells                           | `edit_notebook` (Phase 4.3) — Zod-validated `path` + `operation` (`replace_source` / `insert` / `delete` / `change_type`) + `cell_index` (0-based; `insert` allows `cells.length` to append) + optional `new_source` (required for `replace_source` + `insert`) + optional `new_cell_type` (`code` / `markdown`; required for `insert` + `change_type`). Normalizes `source` to `string[]` on write (canonical Jupyter shape). Returns `{ ok: true, cells: <count> }` on success; surfaces validation / parse / read / write failures as `{ error: ... }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Full**              |
| AskUserQuestion             | Multiple-choice clarifying questions with previews    | `ask_user_question` (Phase 4.1) — Zod-validated `question` + 2–26 `{ label, preview? }` options + optional `allow_free_text` / `timeout_ms` (default 300 000, clamped to 600 000). Library-assigned UUID `questionId` + auto-assigned option ids `'a'`–`'z'`. Wired via `OpenRouterAgentRun({ onAskUserQuestion })`; fires the `Notification` hook with the request payload. Resolves `{ selectedOptionId, label, freeTextAnswer? }` on success; `{ error: 'aborted'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 'timed out after Nms' | 'no host handler registered for ask_user_question' }` otherwise. | **Full** |
| ToolSearch                  | Dynamically load tools on demand from large tool sets | `tool_search` + `tool_load` (Phase 5.5) — opt-in via `OpenRouterAgentRunOptions.enableToolSearch: true`. When set, the MCP bridge's tools are HIDDEN from the model's initial tool pool; the model uses `tool_search({ query, limit? })` to discover them (returns ranked matches with prefixed `serverName__toolName`, originating `server`, `description`, char-capped `schema_preview` with `…` truncation marker, and numeric `score` — name-substring×10, desc-substring×5, name-token×2, desc-token×1; tie-break ascending by name). `tool_load({ names })` then registers one or more by-name into the per-run working set; the wrapped tool is appended to a shared mutable `toolsForRun` array so subsequent turns within the SAME `callModel` cycle can call it (per-cycle granularity is preserved as a fallback). Each successful load fires a `Notification` hook (`level: 'info'`, `message: 'tool_loaded'`, `context: { name, server }`) so audit consumers observe the working-set growth. Loaded-tool state is per-run and does NOT propagate to spawned subagents. | **Full**              |
| TaskCreate / TaskUpdate     | Track subtasks within agent execution                 | `task_create` / `task_update` (Phase 4.2) — Zod-validated `content` + optional `activeForm` on create; `taskId` + 4-state enum (`pending`/`in_progress`/`completed`/`cancelled`) + optional `content` on update. Library-assigned UUID id; ephemeral per-run list (NOT persisted in `state.json`). Both tools emit the full latest list via the `Notification` hook (`message: 'tasks_changed'`) and via the optional `onTasksChanged` constructor callback. `task_update` returns `{ error: 'unknown task id: <id>' }` for unknown ids; otherwise `{}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Full**              |

### Permissions & Safety

| Feature                  | Claude Agent SDK                                                          | OpenRouter Agent Harness                                                                                                                                                                                                                                                                 | Parity      |
| ------------------------ | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Permission modes         | 6 modes: default, dontAsk, acceptEdits, bypassPermissions, plan, auto     | `permissionMode` constructor option — 4 of 6 modes implemented (`default` / `acceptEdits` / `bypassPermissions` / `plan`). `dontAsk` and `auto` not implemented. Composes with the `allowedTools`/`disallowedTools` layer (Phase 3.2).                                                 | **Partial** |
| Allowed/disallowed tools | `allowedTools`, `disallowedTools` with scoped rules (e.g., `Bash(npm *)`) | `allowedTools` / `disallowedTools` constructor options accept plain names and scoped `<Tool>(<pattern>)` rules. Bash patterns use `*` as a wildcard; Edit/Read/Write/List/Grep patterns are path globs. Layers on top of `permissionMode`; explicit `canUseTool` overrides both lists. | **Full**    |
| canUseTool callback      | Runtime approval/deny/modify for each tool call                           | `canUseTool` constructor option — returns `{ behavior: 'allow' \| 'deny', reason?, updatedInput? }`. Server-side tools bypass.                                                                                                                                                         | **Full**    |
| Plan mode (read-only)    | Restricts to read-only tools; explores before editing                     | `permissionMode: 'plan'` (Phase 3.3) denies `write_file` / `edit_file` / `run_command` with reason `'plan mode: read-only — propose edits in your reply'`; allows `read_file` / `list_directory` / `grep_files`. `allowedTools` composes on top to whitelist individual custom tools.  | **Full**    |

### Hooks System

| Feature                  | Claude Agent SDK                                           | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Parity      |
| ------------------------ | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| PreToolUse / PostToolUse | Block, modify, log, audit tool calls with matcher patterns | `onHook` fires `PreToolUse` before the `canUseTool` decision (audit even on deny) and `PostToolUse` after each tool result (`isError` matches subsequent `tool_result.isError`). Phase 3.7: `PreToolUse` may return `{ action: 'block', reason }` (synth-deny shape-identical to `canUseTool`-deny) or `{ action: 'modify', input }` (substituted input flows into `canUseTool` and the underlying tool). `void` return stays continue (backward-compat). Hook-block beats `canUseTool`-allow; `canUseTool`-deny beats hook-continue/modify. | **Full**    |
| Session lifecycle hooks  | SessionStart, SessionEnd, Stop, Setup, Notification        | `onHook` fires `Setup` → `SessionStart` → … → `SessionEnd` → `Stop` on every run; `Setup` / `Stop` still bracket abort and constructor-throw paths. `Notification` is caller-emitted via `ctx.notify` or direct `onHook`.                                                                                                                                                                                                                                                                                                                    | **Full**    |
| Subagent hooks           | SubagentStart, SubagentStop                                | `SubagentStart` + `SubagentEnd` fire on the parent's `onHook` whenever the built-in `spawn_subagent` tool runs (Phase 4.7). Both events carry `parentSessionId`, `subagentSessionId`, `depth`; `SubagentEnd` adds the full `SubagentResultSummary` (status / usage / costUsd / durationMs / reason + concatenated assistant text). Both fire even on the depth-cap rejection path so audit consumers see a matched Start/End pair. Matcher patterns (Claude SDK's per-subagent-name filtering) are not yet implemented.                      | **Partial** |
| Notification hook        | Forward agent status to Slack/PagerDuty/etc.               | `Notification` event in the `HookEvent` union; emit via `ctx.notify(level, message, context?)` from inside a tool, or call `onHook` directly. No-op when `onHook` is omitted.                                                                                                                                                                                                                                                                                                                                                                | **Full**    |
| PreCompact hook          | Archive transcript before compaction                       | Phase 5.1 [✅ #97]: `PreCompact` event added to `HookEvent`. Payload is `{ event, messages: <prefix being condensed>, keepRecentTurns: number, reason: 'auto' \| 'manual' }`. Fires audit-only (return value ignored, throws logged + swallowed) immediately before the isolated summarizer `callModel` runs.                                                                                                                                                                                                                                | **Full**    |

### Subagents & Orchestration

| Feature                    | Claude Agent SDK                                         | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Parity   |
| -------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Subagent spawning          | Agent tool: programmatic or filesystem-based definitions | `spawn_subagent` built-in tool (Phase 4.7) — Zod-validated `description` (prompt) + optional `tools?: string[]` (whitelist that narrows the inherited parent pool — unknown names silently dropped) + `instructions?` / `max_turns?` / `max_budget_usd?` overrides. Spawns a child `OpenRouterAgentRun` with `sessionId = <parentSessionId>:sub:<uuid>`, inheriting parent's `apiKey` / `baseUrl` / `appTitle` / `logsRoot` / `logger` / `onHook` / `model` / `cwd` / `persistSession`. Composite abort signal cascades from parent through the chain. Recursion cap `maxSubagentDepth = 3` (configurable) — chain of at most three levels. Subagent events do NOT bleed into the parent's `for await` — captured internally, surfaced as a single `tool_result`. Opt-in via `OpenRouterAgentRun({ enableSubagents: true })` or `AllToolsOptions.spawnSubagent`. Filesystem-based agent definitions (Claude SDK's `.claude/agents/*.md`) are out of scope (host responsibility).                                                                                                                                                                                                                                                                                                   | **Full** |
| Subagent tool restrictions | Per-subagent tool/model/effort overrides                 | Phase 4.8: `spawn_subagent` input schema accepts `tools?: string[]` (pool-narrowing whitelist, 4.7), `instructions` / `max_turns` / `max_budget_usd` (4.7), plus per-subagent `model`, `permission_mode` (`'default'` / `'acceptEdits'` / `'bypassPermissions'` / `'plan'`), `allowed_tools` / `disallowed_tools` (reuses the Phase 3.2 rule grammar), and `effort` (Phase 5.4: typed enum `'xhigh' \| 'high' \| 'medium' \| 'low' \| 'minimal' \| 'none'`, forwarded into the child's `callModel` request as `reasoning: { effort }`). Override semantics are REPLACE (not COMPOSE) — parent's filters do NOT bleed into the child, so `permission_mode: 'plan'` from a `bypassPermissions` parent isolates the restriction to the child. Composition layers: 4.7 `tools` narrows the pool → 3.1 `permission_mode` gates by tool name → 3.2 `allowed_tools` / `disallowed_tools` layer scoped rules.                                                                                                                                                                                                                                                                                                                                                                              | **Full** |
| Parallel subagents         | Multiple subagents run concurrently                      | `spawn_subagents` (plural, Phase 4.9) — bundled alongside the singular tool whenever `OpenRouterAgentRun({ enableSubagents: true })` is set. Zod-validated `subagents: Array<spec>` (1 to `MAX_PARALLEL_BATCH_SIZE` = 16) where each spec mirrors the singular tool's per-spec schema (per-spec `model` / `permission_mode` / `allowed_tools` / `disallowed_tools` / `effort` / `tools` / `instructions` / `max_turns` / `max_budget_usd` overrides propagate independently). Inline promise pool (no `p-limit` dep) honors `OpenRouterAgentRun({ maxParallelSubagents })` (default `4`); excess specs queue and submit in array order. No fail-fast — per-child failures isolate into `{ status: 'success' \| 'error' \| 'aborted', subagentSessionId, output, error? }` envelopes. Aggregate cost / tokens sum SUCCESS envelopes only. Parent abort fans out into every in-flight child via `AbortSignal.any([parentSignal, internalCtl.signal])` per spec; aborted children surface `status: 'aborted'`. Recursion-depth gate reuses the singular tool's check — a depth-N parent at the cap rejects each spec with the same `'max subagent depth (N) exceeded'` error envelope. Returns `{ results, aggregatedUsage: { usd, tokensIn, tokensOut, totalTokens }, durationMs }`. | **Full** |

### Sessions & State

| Feature                      | Claude Agent SDK                                                            | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Parity      |
| ---------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Session persistence          | JSONL on disk under `~/.claude/projects/`                                   | `ConversationState` (`previousResponseId`) persisted to `<logsRoot>/<sessionId>/state.json` via `FileStateAccessor`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Partial** |
| Session resume               | `resume` by ID, `continue` most recent, `fork` to branch                    | `sessionId` constructor arg — host passes the same id to resume; library round-trips `previousResponseId` per turn                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Partial** |
| Session forking              | Fork creates new session with copied history                                | `forkSession({ sessionId, newSessionId?, logsRoot })` + `OpenRouterAgentRun.fork({ newSessionId? })` (Phase 4.5). Copies the source `state.json` (atomic write — `*.tmp` then `rename`) into a new session directory under the same `logsRoot` and stamps a fresh `session.json` with `parentSessionId` set. Per-request `req_*` / `gen_*` subdirectories are NOT copied — the fork inherits the OR `previousResponseId` chain via `state.json`. Rejects with `cannot fork in-memory session: ...` when the source has no on-disk state. Consumers pass `parentSessionId` to the next `OpenRouterAgentRun` to record lineage (surfaced on `session.json` and on the `session_started` event payload).                                                                                                                                                             | **Full**    |
| Session listing / management | `listSessions()`, `getSessionMessages()`, `renameSession()`, `tagSession()` | Not implemented in-library. The callboard `OpenRouterSessionProvider` concept (Phase 2 in companion plan) scans `<logsRoot>/<id>/session.json` externally — Phase 1.6 captures `cwd` there for that purpose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **None**    |
| File checkpointing           | Track & rewind file changes to any checkpoint                               | `createCheckpoint(sessionId, logsRoot, files[], { logger? })`, `listCheckpoints(sessionId, logsRoot)`, `restoreCheckpoint(checkpointId, sessionId, logsRoot)` (Phase 4.6). Auto-checkpoint hook on `write_file` + `edit_file` via `checkpoint: true` ctor option (per-call override on tool input wins). Snapshots land at `<logsRoot>/<sessionId>/checkpoints/<id>/`: `manifest.json` + one `<encoded-path>.snapshot` per file (path encoding replaces `/` with `__SLASH__`). Tombstones (`existed: false`) handle files absent at checkpoint time — restore unlinks them. Restore is atomic via two-phase rename through `.restore-tmp/`. Per-session cap `MAX_CHECKPOINTS_PER_SESSION = 100` evicts oldest on overflow. Fast-path: identical mtime+size reuses prior snapshot via `fs.link` hard-link. `persistSession: false` interaction = no-op + warn log. | **Full**    |
| persistSession: false        | In-memory only sessions (no disk writes)                                    | `persistSession: false` constructor option swaps `FileStateAccessor` for an in-memory accessor and skips every write under `logsRoot` (`session.json` / `request.json` / `response.json` / `state.json`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Full**    |

### Extensibility

| Feature                     | Claude Agent SDK                                                                    | OpenRouter Agent Harness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Parity   |
| --------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| MCP server support          | stdio, HTTP/SSE, in-process SDK servers; `.mcp.json` config                         | Phase 5.2.\* series [✅ #115 / #116 / #117 / #118 / #103]: `McpStdioClient` (5.2.1) spawns + speaks JSON-RPC over a subprocess's stdio; `McpHttpClient` (5.2.2) speaks MCP over Streamable HTTP and legacy SSE; `loadMcpConfig()` (5.2.3) discovers `.mcp.json` in user + project scopes with deeper-dir override semantics; `McpBridge` (5.2.4) wires each server's tools into `OpenRouterAgentRun` under prefixed names `<serverName>__<toolName>`, routing dispatches back to the originating client; 5.2.5 ships `McpServerStart` / `McpServerStop` lifecycle hooks (capability counts on start, `reason ∈ {'closed', 'error', 'aborted'}` + `durationMs` on stop) and the integration test suite. SDK loaded lazily — users without configured MCP servers pay zero cold-start cost. Init failures surface via `Notification`/`mcp_server_failed` and DO NOT crash the run.                                                                                                                                                                                                                                                                                                                                                  | **Full** |
| Custom tools                | `tool()` helper + `createSdkMcpServer()` — in-process custom tools with Zod schemas | `tools` constructor option accepts any `readonly Tool[]`; bundled `allTools(ctx)` is the default. `tool({ name, description, inputSchema, execute })` (Zod-typed input) and `createSdkMcpServer({ name, version, tools })` (in-process value bag) shipped in Phase 3.5. Real MCP transports (stdio / HTTP+SSE / `.mcp.json` discovery) graduated this row to Full after Phase 5.2.5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Full** |
| Skills system               | Markdown-based skills in `.claude/skills/`                                          | Phase 5.7 [✅ #108]: `createSkillLoader({ cwd, home, pluginRoots })` walks `<cwd up to .git>/.claude/skills/<name>/SKILL.md` + `<home>/.claude/skills/<name>/SKILL.md` + caller-supplied plugin roots (namespaced `<plugin>:<skill>`). Frontmatter parsed via a hand-rolled YAML subset + Zod (`SkillFrontmatter` mirrors the agentskills.io spec + Claude Code's extensions). `OpenRouterAgentRun({ skills, skillsDir, skillDescriptionBudget, disableSkillShellExecution, skillEnv })` injects a `## Available Skills` listing into the instructions and exposes a `skill({ name, arguments? })` built-in tool. Substitution helper handles `${VAR}` / `$ARGUMENTS` / `$N` / `$name` / `` !`cmd` `` (single-pass; output NOT re-scanned). `context: fork` routes through the Phase 4.7 subagent runner; per-skill `allowed-tools` narrows the run-level permission policy for the render's duration. Each successful render fires `Notification(info, skill_loaded, { name, source })`.                                                                                                                                                                                                                                         | **Full** |
| Slash commands              | Custom commands in `.claude/commands/`                                              | Phase 5.6 [✅ #107]: `createCommandLoader({ cwd, home, pluginRoots, skillLoader? })` walks `<cwd up to .git>/.claude/commands/*.md` + `<home>/.claude/commands/*.md` + caller-supplied plugin roots (namespaced `<plugin>:<name>`; subdirs as `<dir>:<name>`). Frontmatter is optional and shares the Phase 5.7 parser + Zod schema (`SkillFrontmatter`). Commands are HOST-invoked: `loader.resolve('foo bar "baz qux"')` shell-splits the input, renders the body via the shared substitution helper (`${VAR}` / `$ARGUMENTS` / `$N` / `$name` / `` !`cmd` `` — `disableSkillShellExecution` honored), and returns `{ name, args, body }`. The host feeds `body` as the next prompt to `OpenRouterAgentRun({ prompt })`. Converged-menu fold (opencode pattern): when `skillLoader` is supplied, `list()` ALSO surfaces every skill as a command of `source: 'skill'`; on same-name collision the command wins. Unknown command → `undefined` (host shows "no such command", does not throw).                                                                                                                                                                                                                                   | **Full** |
| Plugins                     | Extend with custom commands, agents, MCP servers                                    | Phase 5.8 [✅ #109]: `loadPlugins({ pluginDirs, home?, logger? })` → `LoadedPlugin[]` (`{ manifest, root, dataDir, skillRoots, commandRoots, agentRoots, hookConfigs, mcpServers }`). Optional `.claude-plugin/plugin.json` manifest (Zod-validated; unknown keys pass through); auto-discovery from directory name when absent. Per-component path resolution honors Claude Code's replace-vs-add table (skills add to default; commands/agents/hooks/mcpServers replace). `OpenRouterAgentRunOptions.plugins?` ctor opt folds contributions into the skill loader (additive `skillsDir` on `pluginRoots`), MCP bridge (`<pluginName>:<serverName>` namespacing — composes with the bridge's `<serverName>__<toolName>` to yield `<pluginName>:<serverName>__<toolName>`), and the substitution context (`${CLAUDE_PLUGIN_ROOT}` / `${CLAUDE_PLUGIN_DATA}` resolved when the active skill came from a plugin). `PluginStart` / `PluginStop` lifecycle hooks bracket every plugin (counts on Start; `durationMs` + `reason: 'closed'` on Stop). v1 defers marketplace fetcher, `userConfig` runtime, `dependencies`, `${CLAUDE_PLUGIN_DATA}` cleanup, themes/monitors, LSP, `bin/` PATH injection, plugin hook command execution. | **Full** |
| CLAUDE.md / project context | Loaded from `.claude/` and `~/`, configurable via `settingSources`                  | `settingSources` constructor option (Phase 3.4): walks `project` (cwd + .claude/CLAUDE.md up to first .git or 10 levels), `user` (`~/.claude/CLAUDE.md`), and `local` (`<cwd>/.claude/CLAUDE.local.md`); composed into `instructions` in user→project→local order with a ~50k-char cap. Default `[]` preserves verbatim `instructions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Full** |

### Diagnostics & Accounts

| Feature                | Claude Agent SDK                                 | OpenRouter Agent Harness                                                                                                       | Parity  |
| ---------------------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ------- |
| Account / credit info  | Anthropic billing surfaced via console / API key | `accountInfo({ apiKey })` → `{ provider: 'openrouter', label, usageUsd, limitUsd }` (Phase 1.8). Returns `null` for 401/403. | **N/A** |
| Supported models query | Models surfaced via Anthropic console            | `supportedModels({ apiKey })` → `{ value, displayName, description }[]` against `/api/v1/models` (Phase 1.8)                 | **N/A** |

These rows mark provider-specific surface area without a directly-comparable Claude SDK feature; they're listed for completeness, not counted toward parity totals.

---

## Required Features for Parity — Prioritized

> **P0** = core agent capability gaps
> **P1** = important for production use
> **P2** = nice to have / advanced

Items shipped in Phase 1 are crossed through with a back-pointer; the **layer-on-top** gap (e.g., the named-modes layer above `canUseTool`) is what remains in scope.

### P0 — Critical

1. **Permission modes layer.** Named modes (`default` / `acceptEdits` / `bypassPermissions` / `plan`) and an `allowedTools`/`disallowedTools` filtering syntax (e.g., `Bash(npm *)`) on top of the existing primitive. _`canUseTool` runtime callback shipped in Phase 1.4 — the named-modes layer would compose on top._

2. ~~**Context compaction.** Detect context-window pressure and automatically summarize older messages.~~ _Shipped in Phase 5.1 — [✅ #97] `src/compaction.ts` exposes `getModelContextWindow()` + `partitionMessages()` + `COMPACTION_PROMPT`; `OpenRouterAgentRun` gains `compactionThreshold` / `keepRecentTurns` / `autoCompact` ctor opts, a public `compact()` method, and the `PreCompact` lifecycle hook. Char-length heuristic for v1 (no tokenizer dep)._

3. ~~**Hooks (PreToolUse / PostToolUse).**~~ _Shipped in Phase 1.7 via `onHook` (audit-only)._ Block-and-modify capability moves to P1.

4. ~~**Glob tool.** File pattern matching (e.g., `**/*.ts`) separate from `list_directory`.~~ _Shipped in Phase 3.11 — [✅ #50] `src/tools/glob.ts` exposes a `glob` client tool with `**`/`*`/`?`/`[a-z]` patterns, optional `case_sensitive`, BFS walk, 1000-match cap, sorted output, and skips `node_modules`/`dist`/`coverage`/hidden._

5. ~~**AskUserQuestion tool.** Structured multiple-choice clarifying questions during execution.~~ _Shipped in Phase 4.1 — [✅ #52] `src/tools/ask-user-question.ts` exposes `ask_user_question`, wired via `OpenRouterAgentRun({ onAskUserQuestion })` and surfacing every request through the `Notification` hook._

6. ~~**CLAUDE.md / project-context auto-discovery.**~~ _Shipped in Phase 3.4 via the `settingSources` constructor option ([#TBD](https://github.com/WolpertingerLabs/openrouter-agent-harness/pull/TBD))._

### P1 — Important

7. ~~**Subagent system.** Agent tool for spawning focused subtasks with isolated context, restricted tools, optional model overrides.~~ _Basic-sequential shipped in Phase 4.7 — [✅ #58] `src/tools/spawn-subagent.ts` exposes `spawn_subagent`, wired via `OpenRouterAgentRun({ enableSubagents: true })`. Per-subagent model / effort overrides are Phase 4.8; parallel execution is Phase 4.9._

8. ~~**Session forking.** Branch a session from existing history to explore alternatives.~~ _Shipped in Phase 4.5 — [✅ #56] `forkSession()` + `OpenRouterAgentRun.fork()` copy `state.json` into a new session directory and stamp `parentSessionId` for lineage._

9. ~~**Streaming input mode.** AsyncGenerator-based input for mid-session messages, interruptions, image attachments. (Interrupt-only is already covered via `signal` / `abort()`.)~~ _Shipped in Phase 5.3 — [✅ #104] `OpenRouterAgentRunOptions.prompt` widens to `string | AsyncIterable<UserInput>`; `OpenRouterAgentRun.pushUserMessage(msg)` queues follow-up messages; `OpenRouterAgentRun.interrupt()` writes `state.interruptedBy = 'host-interrupt'` so the SDK's between-turn polling exits the in-flight `callModel` cleanly. Image attachments ride on `UserInput.content: ContentBlock[]` (lenient passthrough). Built as the workaround design from [spike 5.S2](./spikes/5.S2-streaming-input.md) — interrupt-then-restart loop hidden behind a Claude-SDK-shaped facade._

10. ~~**Rich message stream.** Typed message objects (SystemMessage, AssistantMessage, UserMessage, ResultMessage) instead of raw stream events. (Partial overlap with the "Streaming output" matrix row.)~~ _Shipped in Phase 3.8 — `run.messages()` returns an `AsyncIterable<AgentMessage>` that aggregates the underlying event stream into typed `SystemMessage` / `AssistantMessage` / `UserMessage` / `ResultMessage` blocks. Existing `for await (... of run)` event stream is unchanged._

11. ~~**MCP server support.** Connect external tools via Model Context Protocol — stdio, HTTP/SSE, `.mcp.json` config.~~ _Shipped via the Phase 5.2.\* series — [✅ #115 / #116 / #117 / #118 / #103] `McpStdioClient` (5.2.1), `McpHttpClient` (Streamable HTTP + SSE; 5.2.2), `loadMcpConfig()` (`.mcp.json` discovery, 5.2.3), `McpBridge` (5.2.4 tool wiring), and `McpServerStart` / `McpServerStop` lifecycle hooks plus the full integration suite (5.2.5). MCP tools surface in the run's tool array under the prefixed name `<serverName>__<toolName>`._

12. ~~**Custom-tools ergonomics.** `tool()` helper / `createSdkMcpServer` / Zod-schema convenience.~~ _Shipped in Phase 3.5 — `tool({ name, description, inputSchema, execute })` (Zod-typed input) and `createSdkMcpServer({ name, version, tools })` (in-process value bag) are exported from the library. Real MCP transports landed in the Phase 5.2.\* series._

13. ~~**Effort / reasoning level.** `effort` option to trade cost vs depth.~~ _Shipped in Phase 5.4 — `OpenRouterAgentRunOptions.effort: EffortLevel` (`'xhigh' | 'high' | 'medium' | 'low' | 'minimal' | 'none'`) forwards into the OR `callModel` call as `reasoning: { effort }` only when set. Per-subagent `effort` override (Phase 4.8) rides the same rails into each child's `callModel` request. OR normalizes the level and maps it to each provider's native param; non-reasoning models silently ignore it._

14. ~~**File checkpointing.** Track file changes and rewind to checkpoints.~~ _Shipped in Phase 4.6 — [✅ #57] `createCheckpoint()` / `listCheckpoints()` / `restoreCheckpoint()` with auto-checkpoint on `write_file` + `edit_file` via `checkpoint: true` ctor option. Per-session cap of 100; hard-link fast path; atomic two-phase restore._

15. ~~**Remaining session lifecycle hooks.** `Stop` (before exit), `Setup` (on first call), `Notification` (forward status externally). `SessionStart` / `SessionEnd` shipped in Phase 1.7 via `onHook`.~~ _Shipped in Phase 3.6 — [✅ #45] `onHook` now fires `Setup` → `SessionStart` → … → `SessionEnd` → `Stop` on every run; `Setup` / `Stop` bracket abort and constructor-throw paths. `Notification` is caller-emitted via `ctx.notify(level, message, context?)` or direct `onHook`._

16. ~~**Block-and-modify hook capability.**~~ _Shipped in Phase 3.7 — `PreToolUse` may return `{ action: 'block' | 'modify' | 'continue' }`. Backward-compatible (void still works). See parity matrix row above for precedence semantics._

17. ~~**Enhanced Bash tool.** Add description field, configurable timeout, improved output handling.~~ _Shipped in Phase 3.9 — optional `description` (advisory free-text from the model, forwarded via `tool_call.input`) and `timeout_ms` (default 30s, clamped to `MAX_TIMEOUT_MS = 600_000` with a `warn` notification on over-cap). SIGTERM/SIGKILL grace is now applied on the timeout path too (matching the existing abort path)._

18. ~~**Enhanced Grep tool.** Add context lines (`-A`/`-B`/`-C`), file-type filters, output modes (content/files/count).~~ _Shipped in Phase 3.10 — `grep_files` now accepts `before_context` / `after_context` / `context` (each capped at 20 lines/side, silently clamped), `type` for built-in filetype aliases (`'ts'`, `'js'`, `'py'`, `'rust'`, `'go'`, `'java'`, `'rb'`, `'php'`, `'c'`, `'cpp'`, `'cs'`, `'sh'`, `'md'`, `'json'`, `'yaml'` — unknown values silently ignored, unions with `file_glob`), and `output_mode` (`'content'` default / `'files_with_matches'` / `'count'`). MAX_MATCHES = 200 honored across all modes ([✅ #49](https://github.com/WolpertingerLabs/openrouter-agent-harness/issues/49))._

### P2 — Nice to Have

19. ~~**Monitor tool.** Watch background processes and react to output lines as events.~~ _Shipped in Phase 4.4 — [✅ #55] `monitor` tool spawns `/bin/sh -c command` and streams stdout+stderr through `readline.createInterface` into a `{ stream, text }` buffer with optional `pattern` filter. Caps via `max_lines` (default 1k, ceiling 10k) and `max_duration_ms` (default 60s, ceiling 600s); forced stops SIGTERM with 250ms SIGKILL grace._

20. ~~**NotebookEdit tool.** Edit Jupyter notebook cells.~~ _Shipped in Phase 4.3 — [✅ #54] `edit_notebook` supports `replace_source` / `insert` / `delete` / `change_type` operations against `.ipynb` files. Normalizes `source` to `string[]` on write (canonical Jupyter shape)._

21. **ToolSearch.** Dynamic tool loading from large MCP tool sets to save context. _Phase 5.5 [✅ #NN] — opt-in via `enableToolSearch: true`. Hides bridge tools until the model invokes `tool_search` to discover them and `tool_load` to register them into the run's working set; each load fires `Notification(info, 'tool_loaded')`._

22. ~~**TaskCreate / TaskUpdate.** Built-in task tracking for multi-step work.~~ _Shipped in Phase 4.2 — [✅ #53] `src/tools/tasks.ts` exposes `task_create` / `task_update`, wired via `OpenRouterAgentRun({ onTasksChanged })`. Both tools share a per-run in-memory list and emit the full latest list through the `Notification` hook (`message: 'tasks_changed'`)._

23. ~~**Skills system.** Markdown-based reusable capabilities in `.claude/skills/`.\_~~ _Shipped in Phase 5.7 [✅ #108] — `createSkillLoader({ cwd, home, pluginRoots })` walks the `agentskills.io`-spec `<root>/.claude/skills/<name>/SKILL.md` shape across user + project + plugin scopes; `OpenRouterAgentRun({ skills, skillsDir, skillDescriptionBudget, disableSkillShellExecution, skillEnv })` injects a `## Available Skills` listing into the system instructions and exposes a `skill({ name, arguments? })` built-in tool. Substitution covers `${VAR}` / `$ARGUMENTS` / `$N` / `$<name>` / `` !`cmd` `` (single-pass; output NOT re-scanned). `context: fork` routes through the Phase 4.7 subagent runner; per-skill `allowed-tools` narrows the run-level permission policy for the render's duration._

24. ~~**Slash commands.** Custom commands in `.claude/commands/`.~~ _Shipped in Phase 5.6 [✅ #107] — `createCommandLoader({ cwd, home, pluginRoots, skillLoader? })` walks the `<root>/.claude/commands/*.md` shape across project + user + plugin scopes (project>user; plugin always namespaced `<plugin>:<name>`; subdirs as `<dir>:<name>`). `loader.resolve('/foo bar "baz qux"')` shell-splits the input, renders the body via the shared substitution helper, and returns `{ name, args, body }` — host feeds `body` as the next prompt. Converged-menu fold surfaces skills as `source: 'skill'` commands when a `skillLoader` is wired in (opencode pattern); command wins on name collision._

25. ~~**Plugins.** Extension system for custom commands, agents, MCP servers.~~ _Shipped in Phase 5.8 [✅ #109] — `loadPlugins({ pluginDirs, home?, logger? })` resolves a directory list to `LoadedPlugin[]` aggregates. Manifest at `.claude-plugin/plugin.json` is optional (auto-discovery from directory name when absent); Zod-validated with unknown keys passing through for forward-compat. Component path resolution honors Claude Code's replace-vs-add table (skills add to default; commands/agents/hooks/mcpServers replace). `OpenRouterAgentRunOptions.plugins?` folds contributions into the skill loader + MCP bridge + substitution context; `PluginStart` / `PluginStop` lifecycle hooks bracket every plugin. v1 defers marketplace fetcher, `userConfig` keychain runtime, `${CLAUDE_PLUGIN_DATA}` cleanup, plugin hook command execution, themes/monitors, LSP, `bin/` PATH injection._

26. ~~**Plan mode.**~~ _Shipped in Phase 3.3 via `permissionMode: 'plan'`._

27. ~~**In-memory sessions.** `persistSession: false` for stateless/ephemeral usage.~~ _Shipped in Phase 3.12 — [✅ #51] `persistSession: false` constructor option swaps `FileStateAccessor` for an in-memory accessor and skips every write under `logsRoot` (`session.json` / `request.json` / `response.json` / `state.json`)._

---

## Sources

- [Claude Code Agent SDK Overview](https://code.claude.com/docs/en/agent-sdk/overview)
- [Agent SDK Hooks](https://code.claude.com/docs/en/agent-sdk/hooks)
- [Agent SDK Permissions](https://code.claude.com/docs/en/agent-sdk/permissions)
- [Agent SDK Sessions](https://code.claude.com/docs/en/agent-sdk/sessions)
- [Agent SDK Subagents](https://code.claude.com/docs/en/agent-sdk/subagents)
- [Agent SDK User Input](https://code.claude.com/docs/en/agent-sdk/user-input)
- [Agent SDK MCP](https://code.claude.com/docs/en/agent-sdk/mcp)
- [Agent SDK Custom Tools](https://code.claude.com/docs/en/agent-sdk/custom-tools)
- [Agent SDK Agent Loop](https://code.claude.com/docs/en/agent-sdk/agent-loop)
- [Agent SDK File Checkpointing](https://code.claude.com/docs/en/agent-sdk/file-checkpointing)
- [Agent SDK Streaming Input](https://code.claude.com/docs/en/agent-sdk/streaming-vs-single-mode)
- [@anthropic-ai/claude-agent-sdk on npm](https://www.npmjs.com/package/@anthropic-ai/claude-agent-sdk)
